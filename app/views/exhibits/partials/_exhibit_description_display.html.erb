<% ctb = current_thinker.bookmarks.find_by_exhibit_id(exhibit.id)%>

<% if ctb.nil?%>
    <div class="opaqify-container" style="background-color:rgba(255,255,255,0.08);">
<% else %>
    <div class="opaqify-container" style="background-color:rgba(0,255,0,0.07);">
<% end %>

  <div class="container-fluid">
    <div class="row-fluid">

      <div class="span2">
        <div class="blanker-box" style="padding: 13px;">
          <img alt="TheThinker" height="53" width="43" src="/assets/TheThinker.jpg" title="Anyone and Everyone will always respect a Thinker.    Photo: The Thinker by Auguste Rodin" class="thumbnail"/>
          <br>
          <div class="muted" style="font-size: 13px"><small><%=link_to  "#{exhibit.thinker.gsub("-"," ").titleize}","#" rescue "Anonymous" %>
            <br><%= exhibit.created_at.utc.localtime.to_s(:short) %>
          </small></div>
        </div>
      </div>
      <div class="span10">
        <div class="blanker-box" style="padding: 13px;">
          <% unless ctb.nil?%>
            <div class="pull-right">Already Viewed and BookMarked</div>
          <% end %>
          <div class="lead text-info"><em><%= exhibit.title.titleize %></em>
            <div class="muted" style="font-size: 13px">Has been Admired by <strong style="color: white"><%= exhibit.popularity_quotient %></strong> Thinkers&nbsp;|&nbsp;
              <a href="/exhibits/<%= exhibit.id%>#comments">Comments <%= "(#{exhibit.comments.count})"%></a>
            </div></div>
          <div style="color:white;font-size: 13px">
            <%= raw "#{exhibit.construct[0..833]} ..." %></div>
          <div class="pull-left">
          <% unless exhibit.brain_storm.blank?%>
              <div class="blanker-box" style="padding: 9px;">
              <div class="" style="color: white"><strong>BrainStorm - </strong><%= exhibit.brain_storm.first.capitalize %></div>
              </div>
          <% end %>
          </div>
          <div class="pull-right">
            <a href="/exhibits/<%= exhibit.id%>"  class="btn btn-small"><i class="icon-align-justify"></i>&nbsp;Read More</a>
            &nbsp;&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;&nbsp;
            <a href="exhibits/<%= exhibit.id %>#comments" class="btn btn-small" ><i class="icon-comment"></i>&nbsp;Comments</a>
          </div>
          <br>
          <br>
        </div>
      </div>
    </div>
  </div>
<% if ctb.nil?%>
    </div>
<% else%>
    </div>
<% end%>

<br>
